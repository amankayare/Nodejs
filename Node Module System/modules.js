/*  1) Each javascript file is a module means each file 
       is associated with one module object
    2) module is an object
    3) module is not a global object but specific to the file
    4) functions and variables written in a module cant be 
       access from outside by default they are private
*/
console.log(module);